# segredos e estado local — nunca versionar
api/.env
api/.tokens.json
api/party.db
api/party.db-wal
api/party.db-shm
api/party.log

# python
api/.venv/
__pycache__/
*.py[cod]
.mypy_cache/
.pytest_cache/
# metadata do `pip install -e .`, regenerada a cada install
api/bq.egg-info/

# node
web/node_modules/
web/dist/
web/src/types/api.d.ts

# playwright — relatórios, traces e os browsers baixados
web/test-results/
web/playwright-report/
web/blob-report/
web/playwright/.cache/

# o perfil dedicado do Chrome que `start.ps1 -Tv` abre. Fronteira de segurança e não conveniência:
# aqui dentro fica a sessão logada da conta com YouTube Premium — cookies vivos, em texto claro.
.chrome-tv/

# editor / os
.vscode/
.idea/
Thumbs.db
desktop.ini
